This code and attached parameter file are part of a broader analytic system, where data is staged, filtered, and passed into report engines like this one to produce desired outputs, in this case a table report.  We need to generate tbl_02.py adhering to the conventions observed in the above code (things like passed variables and output folder structure) while producing a different report.  Our new input data is attached.  Our new report will summarize surgical encounters by year.  We'll use the wheels out time for grouping our years.  We'll also report min, median, mode, mean and max lengths of surgery encounters.  Return to me tbl_02.py and tbl_02.csv parameter file.